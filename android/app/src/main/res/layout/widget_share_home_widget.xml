<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_share_widget_background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:text="Widget Share"
        android:textColor="#7A5D71"
        android:textSize="12sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_status_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:text="Open Widget Share to sync"
        android:textColor="#9A7C91"
        android:textSize="11sp" />

    <TextView
        android:id="@+id/widget_note_text"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="6"
        android:textColor="#3A2F37"
        android:textSize="18sp"
        android:textStyle="bold"
        tools:text="No note yet" />

    <TextView
        android:id="@+id/widget_refresh_text"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:background="@drawable/widget_share_widget_button_background"
        android:gravity="center"
        android:minWidth="72dp"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:text="Open"
        android:textColor="#6F4E63"
        android:textSize="12sp"
        android:textStyle="bold" />
</LinearLayout>
