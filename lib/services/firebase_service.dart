/// Placeholder for Firebase: Auth, Firestore, and Storage uploads.
///
/// Wire this up when `firebase_core` and related packages are added.
class FirebaseService {
  FirebaseService._();

  static final FirebaseService instance = FirebaseService._();

  // Future<void> uploadWidget(...) async { }
}
